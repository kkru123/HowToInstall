1. 일단 루트로 간다 (cd .. 를 이용 하거나 루트로 가는 명령어입력)

2. mkdir 로컬저장소를 만든다 (mkdir 파일이름)

3.현재 저장소 위치를 파악한다 (pwd)

4. git init을 하여 .git폴더를 만들고 시작한다 ( git init)

5. git pull 원격저장소 주소 를 입력하여 원격 저장소에 있는 파일을 불러온다 ( git pull 원격저장소주소)

6. ls -al을 하여 현재 파일에 있는 숨겨진 파일까지 확인한다 (ls -al)

7. txt파일이든 아무 파일이나 만든다 (윈도우에서 만듬)

8. 생성한 파일을 add하여 staging area로 보낸다 (git add 파일이름.확장자명)

9. add한 파일을 커밋한다 -m "메세지 내용" (git commit -m "메세지내용")

10.리모트 한다 > git remote add oringin(이름) 원격저장소 주소 (git remote add origin 원격저장소 주소)

11.git remote를 입력하여 만든 이름이 뭐인지 확인한다 (git remote)

12.브랜치를 만든다 (git branch -M main)

13. 아까 만든 파일을 push하여 원격저장소에 올린다 (git push -u origin main)

14. 원격 저장소를 확인해 파일이 있는지 확인한다.

